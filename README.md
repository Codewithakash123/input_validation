This program validates email, mobile number, and password using Python regular expressions (re module).
User inputs are taken using input() and checked against predefined regex patterns.
Email validation ensures proper format (username, @, domain).
Mobile number validation checks for valid digit length and numeric characters.
Password validation ensures strength based on rules (length, characters).
